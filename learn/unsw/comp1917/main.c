#include<stdio.h>

#define MAXPOWER 10

//Function Prototype

extern int power(int base,int b);
int main(void){
   int i;
   
   for(i=0;i<MAXPOWER;i++){
      printf("%d %d %d\n",i,power(2,i),power(-3,i));
   }

  f(i);

  return (0);
}   

